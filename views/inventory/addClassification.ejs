<!-- title as h1 -->
 <% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<%- messages() %>
<% if (errors) { %>
  <ul class="notice">
 <% errors.array().forEach(error => { %>
   <li><%= error.msg %></li>
<%  }) %>
 </ul>
<% } %>


<div class="containerForm">
  <form class="loginForm" action="/inv/add-classification" method="post">
    <fieldset >
      
      <label class="formLabel">Classification Name
        <p>Name must be alphabetic characters</p>
        <input type="text" id="classification_name" name="classification_name" required pattern="[^0-9]*"></label>
    
      </fieldset>

      <button type="submit" class="button">Add Classification</button>
      
      
    
  </form>

  
</div>